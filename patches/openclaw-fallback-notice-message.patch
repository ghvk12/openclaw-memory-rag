diff --git a/src/auto-reply/fallback-state.test.ts b/src/auto-reply/fallback-state.test.ts
index 67dbfa97..91fbffa3 100644
--- a/src/auto-reply/fallback-state.test.ts
+++ b/src/auto-reply/fallback-state.test.ts
@@ -245,6 +245,6 @@ describe("fallback-state", () => {
         activeModel: "gpt-5.4",
         attempts: [],
       }),
-    ).toContain("selected openai/gpt-5.5");
+    ).toContain("switched from openai/gpt-5.5");
   });
 });
diff --git a/src/auto-reply/fallback-state.ts b/src/auto-reply/fallback-state.ts
index 9f3146b2..c813f797 100644
--- a/src/auto-reply/fallback-state.ts
+++ b/src/auto-reply/fallback-state.ts
@@ -92,6 +92,15 @@ function buildFallbackAttemptSummaries(attempts: RuntimeFallbackAttempt[]): stri
   );
 }
 
+function isGenericFallbackReasonSummary(reason: string): boolean {
+  const normalized = reason.replace(/\s+\(\+\d+ more attempts\)$/, "").trim();
+  return (
+    normalized === "selected model unavailable" ||
+    normalized === "error" ||
+    normalized === "unavailable"
+  );
+}
+
 /** Builds the visible notice shown when runtime falls back from the selected model. */
 export function buildFallbackNotice(params: {
   selectedProvider: string;
@@ -107,7 +116,10 @@ export function buildFallbackNotice(params: {
     return null;
   }
   const reasonSummary = buildFallbackReasonSummary(params.attempts);
-  return `↪️ Model Fallback: ${active} (selected ${selected}; ${reasonSummary})`;
+  if (isGenericFallbackReasonSummary(reasonSummary)) {
+    return `↪️ Using ${active} (switched from ${selected})`;
+  }
+  return `↪️ Using ${active} (switched from ${selected}; ${reasonSummary})`;
 }
 
 /** Builds the visible notice shown when runtime returns to the selected model. */
@@ -119,9 +131,9 @@ export function buildFallbackClearedNotice(params: {
   const selected = formatProviderModelRef(params.selectedProvider, params.selectedModel);
   const previous = normalizeOptionalString(params.previousActiveModel);
   if (previous && previous !== selected) {
-    return `↪️ Model Fallback cleared: ${selected} (was ${previous})`;
+    return `↪️ Back on ${selected} (was using ${previous})`;
   }
-  return `↪️ Model Fallback cleared: ${selected}`;
+  return `↪️ Back on ${selected}`;
 }
 
 type ResolvedFallbackTransition = {
diff --git a/src/status/status-message.ts b/src/status/status-message.ts
index 6a983c7a..f95b7358 100644
--- a/src/status/status-message.ts
+++ b/src/status/status-message.ts
@@ -1007,10 +1007,17 @@ export function buildStatusMessage(args: StatusArgs): string {
     : null;
 
   const showFallbackAuth = activeAuthLabelValue && activeAuthLabelValue !== selectedAuthLabelValue;
+  const fallbackReason = fallbackState.reason;
+  const genericFallbackReason =
+    !fallbackReason || fallbackReason === "selected model unavailable";
   const fallbackLine = fallbackState.active
-    ? `↪️ Fallback: ${activeModelLabel}${
-        showFallbackAuth ? ` · 🔑 ${activeAuthLabelValue}` : ""
-      } (${fallbackState.reason ?? "selected model unavailable"})`
+    ? genericFallbackReason
+      ? `↪️ Using: ${activeModelLabel}${
+          showFallbackAuth ? ` · 🔑 ${activeAuthLabelValue}` : ""
+        } (switched from ${selectedModelLabel})`
+      : `↪️ Using: ${activeModelLabel}${
+          showFallbackAuth ? ` · 🔑 ${activeAuthLabelValue}` : ""
+        } (switched from ${selectedModelLabel}; ${fallbackReason})`
     : null;
   const commit = resolveCommitHash({ moduleUrl: import.meta.url });
   const versionLine = `🦞 OpenClaw ${VERSION}${commit ? ` (${commit})` : ""}`;
